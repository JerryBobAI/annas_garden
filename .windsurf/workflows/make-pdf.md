---
description: Turn markdown into publication-quality PDF
---

Use the `skill` tool to invoke the `make-pdf` skill from gstack.

Features: proper margins, intelligent page breaks, page numbers, cover pages, running headers, curly quotes, clickable TOC, diagonal DRAFT watermark.
