---
description: Pull data from a web page — extract structured JSON
---

Use the `skill` tool to invoke the `scrape` skill from gstack.

First call prototypes the flow and returns JSON. Subsequent calls route to a codified browser-skill (~200ms). Read-only — for form fills use /browse.
