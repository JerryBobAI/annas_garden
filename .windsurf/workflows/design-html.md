---
description: Turn approved designs into production-quality HTML/CSS
---

Use the `skill` tool to invoke the `design-html` skill from gstack.

Generates production-quality HTML/CSS from approved mockups, CEO plans, or design reviews. Text reflows, heights are computed, layouts are dynamic.
