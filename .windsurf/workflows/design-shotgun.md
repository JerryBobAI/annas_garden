---
description: Generate multiple AI design variants for visual comparison
---

Use the `skill` tool to invoke the `design-shotgun` skill from gstack.

Generate multiple design variants, open a comparison board, collect structured feedback, and iterate.
