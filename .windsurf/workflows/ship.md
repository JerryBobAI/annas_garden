---
description: Ship workflow — test, review, version bump, changelog, PR
---

Use the `skill` tool to invoke the `ship` skill from gstack.

Full workflow:
1. Detect and merge base branch
2. Run tests
3. Review diff
4. Bump VERSION
5. Update CHANGELOG
6. Commit and push
7. Create PR
