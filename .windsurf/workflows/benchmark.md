---
description: Performance regression detection — page load, Core Web Vitals, bundle size
---

Use the `skill` tool to invoke the `benchmark` skill from gstack.

Establishes baselines for page load times, Core Web Vitals, and resource sizes. Compares before/after on every PR. Tracks performance trends over time.
