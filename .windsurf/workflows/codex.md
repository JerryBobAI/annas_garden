---
description: OpenAI Codex CLI — independent code review, challenge mode, or consult
---

Use the `skill` tool to invoke the `codex` skill from gstack.

Three modes:
1. **Review** — independent diff review with pass/fail gate
2. **Challenge** — adversarial mode that tries to break your code
3. **Consult** — ask Codex anything with session continuity
