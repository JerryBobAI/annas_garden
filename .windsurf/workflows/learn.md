---
description: Manage project learnings — review, search, prune, export
---

Use the `skill` tool to invoke the `learn` skill from gstack.

Review, search, prune, and export what gstack has learned across sessions. Useful when asking "didn't we fix this before?"
