---
description: Post-deploy canary monitoring — watch for errors and regressions
---

Use the `skill` tool to invoke the `canary` skill from gstack.

Watches the live app for console errors, performance regressions, and page failures. Takes periodic screenshots, compares against pre-deploy baselines, and alerts on anomalies.
