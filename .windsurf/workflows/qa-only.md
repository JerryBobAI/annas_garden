---
description: Report-only QA testing — find bugs but don't fix them
---

Use the `skill` tool to invoke the `qa-only` skill from gstack.

Systematically tests a web application and produces a structured report with health score, screenshots, and repro steps — but never fixes anything. For the full test-fix-verify loop, use /qa.
