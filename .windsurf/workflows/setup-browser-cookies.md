---
description: Import cookies from your real browser for authenticated testing
---

Use the `skill` tool to invoke the `setup-browser-cookies` skill from gstack.

Opens an interactive picker UI where you select which cookie domains to import. Use before QA testing authenticated pages.
