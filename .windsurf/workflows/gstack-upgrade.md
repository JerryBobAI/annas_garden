---
description: Upgrade gstack to the latest version
---

Use the `skill` tool to invoke the `gstack-upgrade` skill from gstack.

Detects global vs vendored install, runs the upgrade, and shows what's new.
