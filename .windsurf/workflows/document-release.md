---
description: Post-ship documentation update — sync README, CHANGELOG, ARCHITECTURE
---

Use the `skill` tool to invoke the `document-release` skill from gstack.

Reads all project docs, cross-references the diff, updates README/ARCHITECTURE/CONTRIBUTING to match what shipped, polishes CHANGELOG voice, cleans up TODOS, optionally bumps VERSION.
