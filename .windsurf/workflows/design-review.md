---
description: Visual QA — find and fix design inconsistencies, spacing issues, AI slop
---

Use the `skill` tool to invoke the `design-review` skill from gstack.

Finds:
- Visual inconsistencies
- Spacing issues
- Hierarchy problems
- AI slop patterns
- Slow interactions

Then iteratively fixes issues in source code, committing each fix atomically and re-verifying with before/after screenshots.
