---
description: Full safety mode — careful + freeze combined
---

Use the `skill` tool to invoke the `guard` skill from gstack.

Maximum safety when touching prod or debugging live systems. Combines destructive command warnings with directory-scoped edit restrictions.
