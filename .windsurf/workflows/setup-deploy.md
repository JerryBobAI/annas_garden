---
description: Configure deployment settings for /land-and-deploy
---

Use the `skill` tool to invoke the `setup-deploy` skill from gstack.

Detects your deploy platform (Fly.io, Render, Vercel, Netlify, etc.), production URL, health check endpoints, and deploy status commands.
