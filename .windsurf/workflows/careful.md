---
description: Safety mode — warn before destructive commands (rm -rf, DROP TABLE, etc.)
---

Use the `skill` tool to invoke the `careful` skill from gstack.

Warns before rm -rf, DROP TABLE, force-push, git reset --hard, kubectl delete, and similar destructive operations.
