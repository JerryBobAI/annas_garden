---
description: Restore working context from a previous session
---

Use the `skill` tool to invoke the `context-restore` skill from gstack.

Loads the most recent saved state so you can pick up where you left off — even across workspace handoffs.
