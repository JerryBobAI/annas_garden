---
description: Save working context — git state, decisions, remaining work
---

Use the `skill` tool to invoke the `context-save` skill from gstack.

Captures git state, decisions made, and remaining work so any future session can pick up without losing a beat. Pair with /context-restore.
