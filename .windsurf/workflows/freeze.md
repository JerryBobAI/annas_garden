---
description: Restrict edits to a specific directory for the session
---

Use the `skill` tool to invoke the `freeze` skill from gstack.

Blocks edits outside the allowed path. Use when debugging to prevent accidentally "fixing" unrelated code.
